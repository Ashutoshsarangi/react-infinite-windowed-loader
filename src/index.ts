export {
  InfiniteLoader,
  default as default
} from './InfiniteLoader';

export type {
  InfiniteLoaderItem,
  InfiniteLoaderProps,
  InfiniteLoaderRef
} from './InfiniteLoader';

// CSS is automatically imported by the component
// Users don't need to import it separately
